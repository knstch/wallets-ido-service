SERVICE_NAME=wallets-service-test

# Postgres (test DB exposed on localhost:5433 by docker-compose profile "test")
PG_USER=wallets_test
PG_PASSWORD=password
PG_USER=wallets_test
PG_HOST=localhost
PG_PORT=5433

# Redis (test Redis exposed on localhost:6381 by docker-compose profile "test")
REDIS_HOST=127.0.0.1
REDIS_PORT=6381
# NOTE: our docker redis uses only requirepass (no ACL users), so username must be empty (or "default").
REDIS_USERNAME=
REDIS_PASSWORD=password

JWT_SECRET=test-secret
PUBLIC_HTTP_ADDR=5556
JAEGER_HOST=
ENVIRONMENT=test

