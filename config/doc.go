// Package config provides environment-driven configuration loading.
//
// It uses env variables (and optionally `.env.local` / `.env` files) to build a
// strongly typed Config which is then used across the service.
package config

